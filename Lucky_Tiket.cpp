#include <iostream>
#include <cstring>

void main()
{
	setlocale(LC_ALL, "rus");
	int number;
	std::cout << "Enter the number of ticket";
	std::cin >> number;
	for (i = 0, i < number / 2, ++i)
		if([i])

	

	
}